{
	"buildCommand": "flutter pub get && flutter build web --release",
	"outputDirectory": "build/web",
	"installCommand": "git clone https://github.com/flutter/flutter.git --depth 1 /tmp/flutter && export PATH=\"$PATH:/tmp/flutter/bin\"",
	"rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
